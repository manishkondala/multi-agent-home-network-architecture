modules:
  http_2xx:
    prober: http
    timeout: 10s
    http:
      preferred_ip_protocol: ip4   # docker network is v4; the image default prefers v6
      follow_redirects: true
      valid_status_codes: []       # default: any 2xx
