apiVersion: 1

providers:
  - name: pi-fleet
    folder: pi-fleet
    type: file
    disableDeletion: false
    updateIntervalSeconds: 60
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
