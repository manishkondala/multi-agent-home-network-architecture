# Watchdog report — 2026-06-12 19:05 EDT (checks executed ~19:27 EDT)

## Verdict
✅ all healthy — full fleet green post-reboot; resilience test passed.

Context applied (per owner): Pi deliberately rebooted ~18:17 EDT today as a resilience
test — ~39 min uptime is expected, not an anomaly. jsms family must be Exited.

## Checks run

| Check | Result | Threshold / expectation |
|---|---|---|
| SSH reachability (pi-node1) | ✅ up, 39 min uptime (expected post-reboot) | reachable |
| Load average | ✅ 0.43 / 0.39 / 0.73 | sane for Pi 4B |
| CPU temp | ✅ 39.9°C | < 70°C |
| Disk / | ✅ 16G avail (45% used) | > 3GB |
| Memory | ✅ 6.5Gi available | > 500MB |
| Compose containers (13) | ✅ all 13 Up: pihole, pihole-exporter, prometheus, node-exporter, cadvisor, grafana, loki, promtail, blackbox-exporter, homepage, cc-points, stock-frontend, stock-backend | all Up |
| pihole / cadvisor / homepage healthchecks | ✅ (healthy) | healthy |
| jsms family (7 containers) | ✅ all Exited; jsms_worker-au restart policy = `no` (others have none) | all Exited, worker restart=no |
| DNS ad-block (dig @192.168.1.169 doubleclick.net) | ✅ 0.0.0.0 | 0.0.0.0 |
| DNS resolution (dig @192.168.1.169 google.com) | ✅ real IPs (142.251.111.x) | real IP |
| Pi-hole counters (since reboot) | ✅ 153 queries, 32 blocked today | non-empty |
| Prometheus targets | ✅ 10/10 up (incl. 4 blackbox-http probes) | all up |
| probe_success (blackbox) | ✅ =1 for cc-points, stock-frontend, stock-backend /api/health, homepage | all 1 |
| App HTTP :3002 cc-points | ✅ 200 (0.10s) | 200 |
| App HTTP :3003 stock-frontend | ✅ 200 (0.006s) | 200 |
| App HTTP :3004 stock-backend /api/health | ✅ `{"status":"ok"}` | ok |
| Homepage :80 | ✅ 200 (0.008s) | 200 |
| Loki /ready | ✅ ready | ready |
| Loki error scan (last 30 min, job="docker") | ⚠️ benign: 2 streams (see below) | no real errors |
| Grafana /api/health | ✅ database ok, v11.6.1 | ok |
| NTP sync (ntpq -p) | ✅ `*` selected peer (st1 GPS-refclock peer, offset +1.2ms) | `*` peer present |
| Sentinel cron installed | ✅ `*/3` detector + 7AM/7PM email report in crontab | both present |
| Sentinel last-run freshness | ✅ 2026-06-12T23:27:01Z vs host time 23:27:06Z (5s old) | within 3 min |
| incidents/new | ✅ empty | empty |

## Anomalies & likely cause
- **None health-impacting.** Two benign log lines matched the Loki error scan:
  1. `promtail` warn "could not transfer logs" for container `82adc9c2…` — promtail trying to
     tail a container that exited around the reboot (jsms family); self-resolving, cosmetic.
  2. `homepage` `[Error: Parse Error: Data after 'Connection: close']` — a homepage widget's
     HTTP client complaining about a backend's response framing; the page itself serves 200 in
     8ms and its blackbox probe is 1. Cosmetic; worth a glance by `dev` only if it recurs.
- jsms exited containers show recent FinishedAt timestamps (~1h / 11h / 22h ago) — consistent
  with the owner's reboot and prior deliberate stops, not crashes. Compliant; leave stopped.

## Recommended action
- **None required.** Fleet is fully healthy post-reboot.
- Optional (dev, low priority): if the homepage `Parse Error` line keeps appearing in Loki,
  identify which widget endpoint emits malformed `Connection: close` framing.
- Optional (infra, cosmetic): promtail "could not transfer logs" warnings for exited jsms
  containers will stop on their own; no action unless they persist past one promtail cycle.
